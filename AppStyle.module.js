import { StyleSheet, Text, View } from 'react-native';

const styles = StyleSheet.create({
    container: {
        position: "relative",
        flex: 1,
        color: 'red',
        alignItems: 'center',
    },
    title: {
        color: 'red'
    }
});
export default styles